import * as jwt from "jsonwebtoken"; 
import { AuthenticatorPayload } from "../Types/authenticator";





export class Authenticator { 
 

    protected static tokenCreator (payload : AuthenticatorPayload){ 
        // jwt.sign()
     
    }
}
